# AI-Suplex Ultra — product preview (static)
#
# The site is PLAIN STATIC HTML/CSS at the repo root (index.html + assets/).
# No framework and no build step: publish the root as-is.
# (The AI Studio Vite/React scaffolding was removed in this commit.)

[build]
  publish = "."
  command = ""
